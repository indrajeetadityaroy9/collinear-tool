{
  "name": "collinear-tool",
  "version": "1.0.0",
  "description": "Collinear API for Vercel deployment",
  "main": "api/index.py",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"
  }
} 